name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install fastapi uvicorn sqlalchemy pytest httpx

      - name: Run tests
        run: |
          pytest tests/test_actors.py -v
        
         
      - name: Run tests
        run: |
          pytest tests/test_actors.py -v --rootdir=.          